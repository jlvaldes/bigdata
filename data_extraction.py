import pandas as pd
from global_variables import CSV_ORIGIN_PATH

def extract_data():
    df = pd.DataFrame()
    path_csv = CSV_ORIGIN_PATH
    
    #poner el código de exracción de datos acá

    return df